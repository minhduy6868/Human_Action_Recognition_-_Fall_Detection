name: video_ai_detect
version: 0.1.0+1
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  http: ^1.2.1
  built_value: ^8.9.2
  built_collection: ^5.1.1
  web_socket_channel: ^2.4.0
  get_it: ^7.2.0
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.11
  built_value_generator: ^8.9.2

flutter:
  uses-material-design: true
