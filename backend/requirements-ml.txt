numpy>=1.23.0,<2.0.0
opencv-python==4.10.0.84
ultralytics==8.3.0
# PyTorch: install CUDA build separately (see below), do NOT pip install from this file for torch
torch==2.4.1
torchvision==0.19.1
scikit-learn==1.5.1
xgboost==2.1.1
pandas==2.2.2
kagglehub>=0.3.0
#
# ── RTX GPU (CUDA) install command ─────────────────────────────────────────────
#   pip install torch==2.4.1+cu121 torchvision==0.19.1+cu121 \
#       --index-url https://download.pytorch.org/whl/cu121
#
#   If NVIDIA driver ≥ 528 you can also use cu124:
#   pip install torch==2.4.1+cu124 torchvision==0.19.1+cu124 \
#       --index-url https://download.pytorch.org/whl/cu124
# ───────────────────────────────────────────────────────────────────────────────
