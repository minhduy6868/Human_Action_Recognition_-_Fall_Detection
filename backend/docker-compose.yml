services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: "video-ai-detect"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres123"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
